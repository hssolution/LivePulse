# meet 소셜 로그인(OAuth) 설정 가이드

> 구글 · 카카오 · 네이버 로그인을 켜는 방법. 실제로 겪은 함정까지 정리.
> 작성: 2026-07-09 / 갱신: 2026-07-10 (**구글·카카오·네이버 3사 모두 설정 완료**)

---

## 0. 핵심 요약 (제일 먼저 볼 것)

- 코드는 이미 **3사 모두 구현 완료**. 개발자센터에서 앱 등록 후 **Client ID / Secret만 `.env`에 넣고 재시작**하면 버튼이 자동으로 켜진다.
- 값이 없으면 버튼은 **자동으로 숨겨진다** (아무 문제 없음).
- **콜백(Redirect) URI는 아래 3개를 그대로** 넣어야 한다. 오타 나면 `redirect_uri_mismatch` / `KOE006`.

| 공급자 | 콜백(Redirect) URI |
|---|---|
| 구글 | `https://meet.noligo.co.kr/api/auth/oauth/google/callback` |
| 카카오 | `https://meet.noligo.co.kr/api/auth/oauth/kakao/callback` |
| 네이버 | `https://meet.noligo.co.kr/api/auth/oauth/naver/callback` |

---

## 1. 서버 적용 방법 (값 받은 뒤 공통)

서버: Tongbig `125.141.139.219`, 앱 경로 `/home/meet/www`, PM2 이름 `meet`.

`.env` 파일에 값 입력 후 재시작:
```bash
cd /home/meet/www
nano .env          # 아래 해당 줄 주석 해제 후 값 입력
pm2 restart meet
```

`.env` 에 들어갈 항목:
```
BASE_URL=https://meet.noligo.co.kr        # (이미 설정됨 — 콜백 URI 조립에 사용)

GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
KAKAO_CLIENT_ID=...        # 카카오 REST API 키
KAKAO_CLIENT_SECRET=...    # 카카오 로그인용 클라이언트 시크릿
NAVER_CLIENT_ID=...
NAVER_CLIENT_SECRET=...
# (선택) KAKAO_SCOPE=profile_nickname   # 기본값. 이메일 동의 붙이면 account_email 추가
```

적용 확인:
```bash
curl -s https://meet.noligo.co.kr/api/auth/providers
# {"ok":true,"providers":{"google":true,"kakao":true,"naver":false}}  ← true 면 켜진 것
```

> ⚠️ **비밀값(Secret)은 이 문서에 적지 말 것.** 실제 값은 서버 `.env`에만 보관. (이 문서엔 "어디서 받는지"만 기록)

---

## 2. 🔵 구글 (Google Cloud Console)  ✅ 설정 완료

주소: **console.cloud.google.com** → "Google 인증 플랫폼(Google Auth Platform)"

1. **프로젝트 생성/선택** (이름: meet)
2. **브랜딩(Branding)**: 앱 이름 `meet`, 사용자 지원 이메일, 개발자 연락처 이메일 입력 → 저장
3. **대상(Audience)**: User type = **External(외부)**, 게시 상태 "테스트"면 **테스트 사용자에 본인 gmail 추가** (심사 없이 테스트 가능)
4. **클라이언트 → OAuth 클라이언트 만들기**
   - 애플리케이션 유형: **웹 애플리케이션**
   - 이름: `meet-web`
   - **승인된 리디렉션 URI**: `https://meet.noligo.co.kr/api/auth/oauth/google/callback`
   - 만들기
5. 생성 팝업에서 **클라이언트 ID**(`...apps.googleusercontent.com`) 복사
6. **클라이언트 보안 비밀(GOCSPX-...)**:
   - 팝업의 **JSON 다운로드** 안에 `client_secret` 있음. **또는**
   - 클라이언트 상세 → **"＋ Add secret"** → 생성 시 딱 한 번 전체값 표시됨 → **바로 복사**
   - ⚠️ 구글은 시크릿을 **생성 순간에만** 보여준다. 놓치면 재생성(Add secret).

**함정 메모**
- 앱이 "테스트" 모드면 **대상에 추가한 테스트 사용자만** 로그인됨. 일반공개하려면 대상에서 **"게시(프로덕션)"**.
- 같은 이메일이면 기존 계정에 자동 연동됨(중복 계정 안 생김).

---

## 3. 🟡 카카오 (Kakao Developers)  ✅ 설정 완료

주소: **developers.kakao.com** → 내 애플리케이션

1. **＋ 앱 생성**: 앱 이름 `meet`, 회사명 입력
2. **제품 설정 → 카카오 로그인 → 일반**: **활성화 상태 ON**
   - OpenID Connect는 **OFF 그대로** (우리는 표준 OAuth 사용)
3. **⭐ Redirect URI 등록 (위치 헷갈림 주의!)**
   - **앱 설정 → 앱 → 플랫폼 키 → "Default Rest API Key" 카드 클릭(열기)** → 그 안에 **Redirect URI** 섹션
   - `https://meet.noligo.co.kr/api/auth/oauth/kakao/callback` 등록 → 저장
   - ※ "카카오 로그인 > 일반" 페이지엔 Redirect URI가 **없다**. UI 개편으로 **플랫폼 키 안**으로 이동됨.
4. **동의항목 설정 (안 하면 KOE205)**
   - **제품 설정 → 카카오 로그인 → 동의항목** → **닉네임(profile_nickname)** → 설정 → **필수 동의**
5. **값 2개** (둘 다 "플랫폼 키 → Default Rest API Key" 안에)
   - **Client ID** = **REST API 키** (예: `02b7c68a...`)
   - **Client Secret** = **클라이언트 시크릿 → "카카오 로그인" 코드** (활성화 ON 상태의 코드)
     - ⚠️ "비즈니스 인증" 코드가 아니라 **"카카오 로그인" 코드**를 써야 함 (2개 나옴)

**함정 메모 (실제 겪음)**
- **이메일(account_email)은 비즈앱(사업자) 심사 필요** → 지금은 이메일 없이 닉네임만 받음.
  - 코드가 이메일 없으면 `kakao_{카카오번호}@meet.local`로 **자동 처리**하므로 로그인은 정상.
  - 나중에 실제 이메일 받으려면: 비즈앱 전환 → account_email 동의항목 설정 → `.env`에 `KAKAO_SCOPE=account_email profile_nickname` 추가.
- **KOE 에러 코드 대응**:
  - `KOE006` = Redirect URI 미등록/불일치 → 3번 다시 확인
  - `KOE205` = 동의항목(닉네임) 미설정 → 4번 설정
  - `KOE205`가 떴다는 건 **Redirect URI는 이미 정상**이라는 뜻(그 검사를 통과했으니).

---

## 4. 🟢 네이버 (Naver Developers)  ✅ 설정 완료

주소: **developers.naver.com** → 상단 **Application → 애플리케이션 등록**

1. **애플리케이션 이름**: `meet` (네이버 로그인 화면에 노출, 10자 이내 권장)
2. **사용 API** — 권한(제공정보) 표에서 체크:
   - **연락처 이메일 주소** → **필수**
   - **별명** → **필수** (또는 **회원이름**을 필수로 — 실명 표시엔 회원이름이 더 정확)
   - ※ 우리 코드는 `이름(name) → 없으면 별명(nickname)` 순으로 씀. 둘 중 하나 + 이메일이면 OK.
   - 💡 표 위의 **"선택하세요"(네이버페이·데이터랩·카페·캘린더…) 드롭다운은 무시** — 로그인만 필요.
3. **로그인 오픈 API 서비스 환경** (빨간 테두리 ⓘ 뜨는 곳) → **"환경 추가" 드롭다운 → "PC 웹" 선택**
   - **서비스 URL**: `https://meet.noligo.co.kr`
   - **네이버 로그인 Callback URL**: `https://meet.noligo.co.kr/api/auth/oauth/naver/callback`
4. **서비스 이용 동의**: 맨 아래 **체크박스 2개 모두 체크** (NAVER API 약관 / 개인정보 활용)
5. 위 3·4 완료돼야 **"등록하기" 버튼이 진한 초록으로 활성화** → 클릭
   - (최초 1회 연락처 인증 — 휴대폰/이메일 중 하나)
6. 등록되면 **내 애플리케이션 → meet → 개요**에서 **Client ID / Client Secret("보기" 클릭)** 복사
7. `.env`에 `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET` 입력 → `pm2 restart meet`

**함정 메모 (실제 겪음)**
- **"등록하기" 버튼이 연한 초록(비활성)** 이면 → ① 서비스 환경(PC 웹 + URL 2개) 미설정 ② 서비스 이용 동의 2개 미체크 → 이 둘 하면 활성화됨.
- 네이버는 **SNS 로그인 검수(심사)** 절차가 있음. **검수 전에도 앱 등록한 본인 네이버 계정으로는 바로 테스트 로그인 가능**. 일반공개는 검수 통과 후.
- 네이버는 이메일 제공이 기본(동의 시)이라 카카오 같은 이메일 문제는 적음.
- Client ID는 언더스코어로 시작하는 20자 내외(예: `_C0Ve...`), Secret은 10자 내외.

---

## 5. 코드 동작 방식 (참고)

- 파일: `src/auth.js` — `OAUTH` 객체에 3사 설정. `https` 모듈로 토큰교환(Node16, fetch 없음).
- 흐름: `/api/auth/oauth/{provider}` → 공급자 동의화면 → `/api/auth/oauth/{provider}/callback` → 토큰교환 → 프로필조회 → 회원 upsert → 세션쿠키 → `/user`.
- 신규 소셜 회원: **이메일 자동 인증(면제)** + 무료 200크레딧 지급. 같은 이메일이면 기존 계정에 연동.
- 프로필 정규화:
  - google: `prof.id / prof.email / prof.name`
  - kakao: `prof.id / prof.kakao_account.email / prof.properties.nickname` (이메일 없으면 합성)
  - naver: `prof.response.{id,email,name}`

---

## 6. 현재 상태 (2026-07-10 · 3사 모두 완료)

| 공급자 | 상태 | 비고 |
|---|---|---|
| 구글 | ✅ 작동 | 테스트 모드(테스트 사용자만). 일반공개 시 대상 "게시" |
| 카카오 | ✅ 작동 | 닉네임만 수신(이메일은 비즈앱 필요) |
| 네이버 | ✅ 작동 | 개발중 상태(본인 계정 테스트 가능). 일반공개는 검수 통과 후 |

`/api/auth/providers` 응답이 `{"google":true,"kakao":true,"naver":true}` 이면 3사 모두 활성.
문제 생기면: `pm2 logs meet` 에서 `[oauth ...]` 에러 확인.

### 실서비스 오픈 전 체크
- **구글**: Google 인증 플랫폼 → 대상 → **"게시(프로덕션)"** 해야 테스트 사용자 외 일반인 로그인 가능.
- **네이버**: 애플리케이션 → **"검수 요청"** 통과해야 일반 공개.
- **카카오**: 기본 로그인은 바로 가능. 이메일까지 받으려면 **비즈앱 전환 + account_email 동의항목**.
